---
layout: home.njk
title: Christopher's Tips
description: A collection of a few things I'd like to remember.
permalink: /posts/
eleventyNavigation:
  key: Archive
  order: 2
---
<section class="hero is-fullheight-with-navbar">
    <div class="hero-body">
      <div class="container">
        <div class="level py-5">
          <div class="columns is-vcentered">
            <div class="column">
              <div class="columns">
                <div class="column">
                  <h1 class="title is-3 has-text-weight-light">Latest Tips</h1>

                  {% set postslist = collections.posts %}
                  {% include "postslist.njk" %}

                </div>
                {% if collections.tagList | length > 0 %}
                  <div class="column is-one-third">
                  <h1 class="title is-3 has-text-weight-light">Tags</h1>

                  <div class="tags">
                  {% for tag in collections.tagList %}
                    {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
                    <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
                  {% endfor %}
                </div>
                {% endif %}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
