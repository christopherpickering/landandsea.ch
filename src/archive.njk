---
layout: home.njk
title: Christopher's Tips
description: A collection of a few things I'd like to remember.
permalink: /posts/
eleventyNavigation:
  key: Archive
  order: 2
---
<section class="grow flex py-6">
      <div class="container mx-auto flex flex-col md:flex-row">

          <div class="w-full md:w-2/3">
                <h1 class="text-2xl font-light text-slate-800 dark:text-slate-200 flex items-center space-x-2">Latest Tips</h1>

                {% set postslist = collections.posts %}
                {% include "postslist.njk" %}

              </div>
              {% if collections.tagList | length > 0 %}
                <div class="w-full md:w-1/3">
                <h1 class="text-2xl font-light text-slate-800 dark:text-slate-200 flex items-center space-x-2 mb-2">Tags</h1>

                <div class="flex flex-wrap gap-1">
                {% for tag in collections.tagList | sort %}
                  {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
                  <a href="{{ tagUrl | url }}" class="rounded-md bg-white/50 border px-2 hover:outline hover:outline-2 hover:outline-offset-2 hover:outline-amber-500/50 text-slate-600 hover:text-slate-900 transition-colors whitespace-nowrap">{{ tag }}</a>
                {% endfor %}
              </div>
              {% endif %}
            </div>

        </div>
      </div>
    </div>
  </div>
</section>
